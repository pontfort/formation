<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:c="http://www.springframework.org/schema/c" 
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context 
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/tx
                           http://www.springframework.org/schema/tx/spring-tx.xsd
                           http://www.springframework.org/schema/aop 
                           http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">
	<context:component-scan base-package="com.formation.emergency" />

	<tx:annotation-driven transaction-manager="txManager" />
	<!-- the transactional advice (what 'happens'; see the <aop:advisor/> bean below) -->
    <tx:advice id="txAdvice" transaction-manager="txManager">
        <!-- the transactional semantics... -->
        <tx:attributes>
            <!-- all methods starting with 'get' are read-only -->
            <tx:method name="get*" read-only="true"/>
            <!-- other methods use the default transaction settings (see below) -->
            <tx:method name="*"/>
        </tx:attributes>
    </tx:advice>
	
	<!-- Import conf DB -->
	<import resource="emergency-db.xml" />
	
	<!-- Import conf DAO -->
	<import resource="emergency-dao.xml" />

	<!-- Import conf SERVICE -->
	<import resource="emergency-service.xml" />
	
	<bean id="myEventHandler" class="com.formation.emergency.aop.EventHandler"></bean>
	<aop:config>
		<aop:aspect ref="myEventHandler">
			<aop:pointcut expression="execution(* com.formation.emergency.business..*.*(..))" id="acceuilService"/>
			<aop:around pointcut-ref="acceuilService" method="aroundExecution"/>
		</aop:aspect>
	</aop:config>
	
	<aop:config>
	    <aop:pointcut id="dao" expression="execution(* com.formation.emergency.business.dao.*Dao*.*(..))"/>
        <aop:advisor advice-ref="txAdvice" pointcut-ref="dao"/>
	</aop:config>
</beans>
                           